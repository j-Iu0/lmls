# Core runtime — enough to run the graph with mock/passthrough adapters and every CLI.
# Verified against PyPI on 2026-09-01.
numpy>=1.26,<3
scipy>=1.13
soundfile>=0.13
sounddevice==0.5.6
typer==0.27.2
rich>=13.7
websockets==17.1

# Noise reduction (spectral gate). Pure-python + numpy/scipy, no model download.
noisereduce==3.0.3

# Benchmarking / evidence of testing
jiwer>=3.1

# Tests
pytest>=8.3
pytest-asyncio>=0.24
