# Optional cloud adapters for CORRECTION and TRANSLATION only. Deepgram speech-to-text
# has its own requirements-deepgram.txt because it does not need either LLM client.
# None of these are needed for the default local-first pipeline.
#   .venv/bin/pip install -r requirements-cloud.txt
anthropic>=0.40       # correct.cloud_llm / translate.cloud_llm
openai>=1.55          # ditto, provider = "openai"
